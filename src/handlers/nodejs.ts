import { HandlerInputType } from '../types';

export default ({ network }: HandlerInputType) => {
  //TODO: implement this
  throw new Error(
    'Nodejs generation failed. Not implemented yet in ' + network
  );
};
